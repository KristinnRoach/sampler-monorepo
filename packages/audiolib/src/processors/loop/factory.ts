// import { createWorkletNode } from '../../base/classes/BaseWorkletNode';
// import { BaseWorkletNode } from '../../base/classes/BaseWorkletNode';
// // todo: register all processors in one place and make the factory function just create the node

// export default function createLoopProcessor(
//   audioContext: BaseAudioContext,
//   options: AudioWorkletNodeOptions = {}
// ): BaseWorkletNode {
//   return createWorkletNode(audioContext, 'loop-processor', options);
// }

// // export default function registerLoopProcessor
