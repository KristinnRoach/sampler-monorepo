// src/API.ts - External API for audiolib

// interface Node {
//     out: NodeID[]; // [], just ctx.destination if root
//     subGraph: Node[] | Map<NodeID, Node[]>;
//     in: NodeID[]; // [], empty if leaf
//     addNode: (node: Node) => boolean;
//     removeNode: (node: Node) => boolean;
//   }
