{
  "name": "@repo/audio-components",
  "version": "0.1.0",
  "description": "Web Components for Audio Processing",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./style": "./dist/audio-components.css",
    "./audio-components.css": "./dist/audio-components.css"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm clean && vite build",
    "watch": "vite build --watch",
    "clean": "rm -rf dist && mkdir -p dist",
    "clean:deps": "rm -rf node_modules && pnpm install",
    "clean:all": "rm -rf dist node_modules && pnpm store prune && pnpm prune"
  },
  "dependencies": {
    "@repo/audiolib": "workspace:*",
    "@repo/vanjs-core": "workspace:*",
    "gsap": "^3.13.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^6.0.0",
    "vite-plugin-dts": "^4.5.3",
    "vitest": "^3.2.4"
  }
}
