{
  "name": "@repo/audio-components",
  "version": "0.1.0",
  "description": "Web Components for Audio Processing",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "vite build",
    "dev": "pnpm clean && vite build --watch",
    "dev:server": "node scripts/dev.js",
    "clean": "rm -rf dist node_modules && pnpm prune"
  },
  "dependencies": {
    "@repo/audiolib": "workspace:*",
    "@repo/vanjs-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^6.0.0",
    "vite-plugin-dts": "^4.5.3",
    "vitest": "^3.0.0"
  }
}
