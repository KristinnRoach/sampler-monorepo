{
  "name": "@repo/sampler-monorepo",
  "version": "0.0.0",
  "devDependencies": {
    "prettier": "^3.4.2",
    "syncpack": "^13.0.2",
    "turbo": "^2.3.4",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.0.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "go": "turbo lint && turbo check-types && turbo build",
    "lint": "turbo lint"
  }
}
