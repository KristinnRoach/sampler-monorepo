{
  "name": "test-elements",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "watch": "vite",
    "build": "pnpm clean && vite build",
    "clean": "rimraf dist && mkdir -p dist",
    "clean:deps": "rimraf node_modules && pnpm install",
    "clean:all": "rimraf dist node_modules && pnpm store prune && pnpm prune",
    "gh-deploy": "gh-pages -d dist -b gh-pages",
    "preview": "vite preview"
  },
  "dependencies": {
    "@repo/audio-components": "workspace:*",
    "@repo/vanjs-core": "workspace:*",
    "animejs": "^4.0.2",
    "gsap": "^3.13.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^6.0.0",
    "vite-plugin-dts": "^4.5.3",
    "vite-plugin-pwa": "^1.0.0",
    "vitest": "^3.0.0"
  }
}
