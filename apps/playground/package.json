{
  "name": "playground",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "watch": "vite",
    "build": "pnpm clean && vite build",
    "clean": "rm -rf dist dev-dist ",
    "clean:deps": "rm -rf node_modules && pnpm install",
    "clean:all": "rm -rf dist node_modules && pnpm store prune && pnpm prune",
    "gh-deploy": "pnpm build && gh-pages -d dist -b gh-pages",
    "preview": "vite preview",
    "generate-pwa-assets": "pwa-assets-generator"
  },
  "dependencies": {
    "@repo/audio-components": "workspace:*",
    "@repo/vanjs-core": "workspace:*",
    "gsap": "^3.13.0"
  },
  "devDependencies": {
    "@vite-pwa/assets-generator": "^1.0.0",
    "typescript": "^5.8.0",
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^1.0.2",
    "vitest": "^3.2.4"
  }
}
